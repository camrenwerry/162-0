# Optional. When unset, feedback links are hidden.
VITE_FEEDBACK_URL=https://example.com/pennant-pursuit-feedback

# Optional for ordinary development. Required only by read-only online Preview
# checking and planning. Use a dedicated, least-privilege Preview read token;
# never reuse a generic Cloudflare credential or commit a real value.
PENNANT_PREVIEW_API_TOKEN=
